Name: Workflow_4e81
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
  TerraformCommunityEdition_96:
    # Identifies the action. Do not modify this value.
    Identifier: codecatalyst-labs/provision-with-terraform-community@v1.0.2
    Environment:
      Connections:
        - Role: CodeCatalystWorkflowDevelopmentRole-dev-ellinj
          Name: "958006911575"
      Name: admin
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Compute:
      Type: EC2
    # Defines the action's properties.
    Configuration:
      # Required; type: string; description: The Terraform operation mode, should one of test or plan or apply
      TerraformOperationMode: plan
      TerraformVersion: 1.5.1
      AWSRegion: us-west-2
      StateBucket: wiz-je-test
      StateKey: wiz-je-test
      WorkingDirectory: s3-backend
      StateTable: wiz-je-test
